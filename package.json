{
  "name": "crypto-heatmap-volume",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev-server.cjs",
    "generate": "node scripts/generate-case-pages.cjs && node scripts/generate-2026-pages.cjs",
    "build": "npm run generate && node scripts/build.cjs"
  }
}
